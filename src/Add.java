
/**
 * Interfaccia che estende NodeComputation.
 * All'interno di Add vi &egrave solamente il metodo add(long first, long second) per l'addizione di due long. 
 * 
 * @author Vincenzo Arceri, Matteo Calabria, Pietro Musoni, Carlo Tacchella 
 */

public interface Add extends NodeComputation {
	public long add(long first, long second);
}
